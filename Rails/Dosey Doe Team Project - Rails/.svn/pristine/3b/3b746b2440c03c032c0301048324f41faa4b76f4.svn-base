require 'rails_helper'

# not sure if this should actually go here, but why not
RSpec.describe ApplicationController, type: :routing do
  describe 'routing' do

    it 'root routes to shows#index' do
      expect(get: '/'). to route_to('shows#index')
    end

  end
end
